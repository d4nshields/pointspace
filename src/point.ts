/**
 * Point encapsulates the data type of a point on a line, with a velocity
 */

export class Point {
    x: number;
    v: number;
    constructor( x, v)
    {
        this.x=x;
        this.v=v;
    }
}
